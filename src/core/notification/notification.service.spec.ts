describe('NotificationService', () => {
    describe('NotifyOverlap', async () => {
        it('should publish a notification into the queue', async () => {
            
        });

        it('should mark an earthquake as notification sent', async () => {
            
        });

        it('should remove notification sent when publish into queue fails', async () => {

        });

        it('should throw an error when marking an earthqueake as notification sent fails', async () => {

        });

        it('should throw an error when publishing into the queue fails', async () => {
            
        })
    })
    
})